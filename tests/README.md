# Tests

Tests and validation code belong here.

Use this folder for user-mode unit tests, shared contract tests, driver interface tests, VM-only integration checks, and test fixtures.

Current foundation files:

- `shared_contract_layout.cpp`: compile-time checks for shared struct sizes and IOCTL uniqueness.
- `app_driver_integration_notes.md`: VM-only integration test checklist for later.
