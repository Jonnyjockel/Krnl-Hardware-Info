# VM Deployment Notes

This project should be tested in a VM.

Future helper scripts can live in this folder for:

- Creating a local test certificate.
- Enabling Windows test-signing mode in a disposable VM.
- Copying the app and driver to a test VM.
- Installing and removing the driver service.
- Capturing driver logs.

TODO: Add exact commands after the WDK project, driver service name, and deployment target are finalized.

